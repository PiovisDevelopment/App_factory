# Faster Whisper STT Plugin Dependencies
# =======================================
# For Windows 11 + RTX 4080 + CUDA 12.9 + Python 3.11

# Core faster-whisper package (CTranslate2 backend)
# 4x faster than openai-whisper, lower VRAM usage
faster-whisper>=1.0.0

# Audio file handling
soundfile>=0.12.0

# Note: PyTorch is NOT required for faster-whisper (uses CTranslate2)
# Note: cuDNN should already be installed with CUDA Toolkit
