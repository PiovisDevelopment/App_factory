# Kokoro TTS Plugin Dependencies
# ===============================
# For Windows 11 + RTX 4080 + CUDA 12.9 + Python 3.11

# Core Kokoro package (includes model auto-download)
kokoro>=0.9.4

# Audio file handling
soundfile>=0.12.0

# G2P (Grapheme-to-Phoneme) for English
misaki[en]

# PyTorch with CUDA support (should already be installed system-wide)
# Uncomment if needed:
# --index-url https://download.pytorch.org/whl/cu129
# torch>=2.9.0
# torchaudio>=2.9.0

# Note: espeak-ng is a SYSTEM dependency (not pip)
# Install from: https://github.com/espeak-ng/espeak-ng/releases
