/**
 * src/index.ts
 * =============
 * Main barrel export for App Factory Phase 1 components.
 *
 * This file re-exports all UI components, context providers,
 * and related types from their respective modules.
 *
 * Usage:
 *   import { Button, Input, ThemeProvider, useTheme } from "@app-factory/ui";
 */

// UI Components (D010-D015, D017-D019)
export * from "./components/ui";

// Context Providers (D016)
export * from "./context";
